/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["media.istockphoto.com", "feeds.abplive.com"],
    // unoptimized: true,
  },
  // output: 'export',
};

export default nextConfig;
